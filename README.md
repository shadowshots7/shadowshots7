```
┌──(shadow㉿shots)-[~]
└─$ id
uid=1337(shadow) gid=0(root) groups=0(root),1337(hackers)

┌──(shadow㉿shots)-[~]
└─$ cat /etc/motd
cybersecurity researcher · ethical hacker · builder of tools

┌──(shadow㉿shots)-[~]
└─$ nmap -sV --open shadow
PORT      STATE  SERVICE          VERSION
443/tcp   open   pentest          active
1337/tcp  open   vuln-research    active
4444/tcp  open   red-team         active
9001/tcp  open   security-tools   active

┌──(shadow㉿shots)-[~]
└─$ ls -la repos/
drwxr-xr-x  pas/  — pentest assessment suite

┌──(shadow㉿shots)-[~]
└─$ uname -a
Linux shadow 6.1.0-kali · shell · skynet (intern)

┌──(shadow㉿shots)-[~]
└─$ cat contact.txt
[*] socials coming soon...

┌──(shadow㉿shots)-[~]
└─$ █
```
